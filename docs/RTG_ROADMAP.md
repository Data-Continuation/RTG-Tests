# RTG Roadmap

## Immediate Next Steps

```text
1. Install repo documentation bundle.
2. Confirm docs-only upload is green.
3. Add a generated test index that can be refreshed from config/rtg_declared_tasks.json.
4. Add machine-readable RTG status metadata.
5. Add versioned baseline receipts.
```

## Near-Term Test Work

Potential next test groups:

```text
observer-class hierarchy tests
detector capability upgrade tests
transition packet schema tests
master-record receipt export tests
ecosystem reentry review tests
quorum/finality split tests
admissibility-pressure function tests
dark-cell visualization metadata tests
```

## Formalism Work

Potential formalism documents:

```text
RTG axioms
RTG glossary
RTG transition packet schema
RTG observer identity classes
RTG detector compatibility model
RTG admissibility pressure functions
RTG false-transition taxonomy
RTG unknown-unknown governance model
```

## Site / Visualization Work

Potential Site outputs:

```text
transition-table color emergence layer
dark-cell confidence visualization
observer-detector compatibility overlay
replay/receipt packet viewer
quarantine/provisional state visualization
cross-layer coherence heatmap
```

## Long-Term Goal

RTG should become a formal, executable, receipt-bearing governance model for transition validity.

The target is not a static table.

The target is a governed geometry of observable, admissible, replayable, authority-bound transitions.
